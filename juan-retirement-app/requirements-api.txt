# FastAPI Backend Dependencies
# For retirement simulation REST API

# Core FastAPI
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0

# Async support
python-multipart==0.0.6

# Data processing (existing dependencies from app.py)
pandas==2.1.3
numpy==1.26.2

# Existing simulation dependencies
# (These should already be installed for app.py)
streamlit==1.28.2  # Keep for compatibility with existing modules
altair==5.1.2
plotly==5.18.0

# Add any other dependencies from your existing modules
# that are imported by the simulation engine
