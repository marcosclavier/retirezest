# Nixpacks Configuration for Railway
# Simple Python deployment

[variables]
PYTHONPATH = "/app/webapp/python-api"

[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["pip install fastapi uvicorn pandas numpy pydantic python-multipart"]

[start]
cmd = "python app.py"
