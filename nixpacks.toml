# Nixpacks config for Railway - Python API
providers = ["python"]

[phases.setup]
nixPkgs = ["python311"]

[phases.install]
# Install dependencies from requirements.txt
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "python app.py"