# E2E Test Environment Variables
# These are loaded when running Playwright tests

# Server-side E2E test mode (bypasses Turnstile verification on backend)
E2E_TEST_MODE=true

# Client-side E2E test mode (bypasses Turnstile widget on frontend)
NEXT_PUBLIC_E2E_TEST_MODE=true
